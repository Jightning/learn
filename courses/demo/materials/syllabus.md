# Syllabus: DEMO 001

Not an institutional course. The sequence is ordered by who needs it: the first
section is everything a reader must know to use the site, and the rest is why it
works that way and what it can render.

| § | Title | For | Features shown |
|---|---|---|---|
| 1.1 | [What a course is](#s1-1) | readers | `p` `def` `key` `note` `list`, sections and subsections |
| 1.2 | [Answering a question](#s1-2) | readers | `attempt` opener, `def` `key` `trap` `ex`, confidence and the reason prompt |
| 1.3 | [Review and drills](#s1-3) | readers | plain `table`, `trap`, how a concept enters the schedule |
| 1.4 | [How much of a page you have to read](#s1-4) | readers | tiers, the lane selector, `mono` table with split, a live stub |
| 1.5 | [Finding your way around](#s1-5) | readers | margin cards, concept links, search and keyboard, install and sync |
| 2.1 | [Completeness and non-redundancy](#s2-1) | authors | the two rules, concept links, subsection cross-ref |
| 2.2 | [How they coexist](#s2-2) | authors | `attempt`, `ex`, `list` (ordered + unordered), cross-course link |
| 3.1 | [The two loops](#s3-1) | authors | `math` block, custom `metric` block, "Spot the error" quiz type |
| 3.2 | [Why tiers, and what the evidence says](#s3-2) | authors | the three tests, the depth constraint, the evidence and its limit |
| 4.1 | [Diagrams](#s4-1) | authors | figure kinds `graph` (circle + layered), `flow` (row + col), `timing`; `<f k>` citation |
| 4.2 | [Charts](#s4-2) | authors | figure kinds `plot` (fn + points + dash), `bar`, `scatter` (trend) |
| 4.3 | [Grids, matrices, escape hatch](#s4-3) | authors | figure kinds `grid` (binary index + groups), `matrix`, `svg` |
| 5.1 | [Tables and code](#s5-1) | authors | `table` plain / `mono` / `map`, `code` with `syntax` highlighting |
| 5.2 | [Media and edge cases](#s5-2) | authors | `image` (with + without caption), `source: unverified`, `source: generated` |

## Course-level features

`course.yaml`: `valueStyles`, custom `styles`, `syntax` (comment / keywords /
patterns / strings), `theme.hue`, `state.enabled`. `blocks.js`: the `metric`
custom renderer. Generated: `problems.md`, `checklist.md`. See also
[schedule](#/demo/) and [reference](#/demo/).
